export const PORT = process.env.PORT ?? 8080;
export const ACCESS_TOKEN = process.env.ACCESS_TOKEN ?? "";
export const CONFIRMATION = process.env.CONFIRMATION ?? "";

console.log({ PORT, ACCESS_TOKEN, CONFIRMATION });
